// Runtime configuration that can be edited after deployment
// This file is loaded at runtime, not build time
window.APP_CONFIG = {
  // Backend API URL - Edit this if deploying to different backend
  API_URL: 'https://smart-video-learning-assistant.onrender.com',
  
  // Alternative: Uncomment to use different backend
  // API_URL: 'https://your-custom-backend.com',
  
  // For local development, the app will use http://localhost:5000
};
